/**
 * Where the source lives.
 *
 * Named once. Three pages invite a reader to go and check something, and a
 * repository address retyped on each of them is a repository address that will
 * eventually disagree with itself.
 */
export const SOURCE = 'https://github.com/zeloric-dev/stratifypro';
